language: node_js
node_js:
  - lts/carbon # version 8
  - lts/dubnium # version 10
  - 12 # version 12
os:
  - windows
  - linux
  - osx
cache: npm
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libstdc++-4.9-dev
